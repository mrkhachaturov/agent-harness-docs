# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Local working directory
.local/

# Graphify temp files (graph output is committed, temp files are not)
graphify-out/.graphify_*
graphify-out/.preprocessed/
graphify-out/cache/

# Project specific
*.log
.claude/
.rkstack/
.last_pull
.last_check
claude-docs-helper.sh

# Sparse-checkout caches used by maintainer fetchers
.cache/

# Node deps for the MDX converter (lockfile IS committed for reproducible CI)
scripts/node_modules/

# Ruff lint/format cache
.ruff_cache/

# mise — local machine override (mise.lock IS committed for reproducible installs)
mise.local.toml

# Local exploration clones (user clones upstream repos here for inspection)
tmp/

# Local-only instructions / overrides (per Claude Code convention)
CLAUDE.local.md
*.local.md

# Test files
test_*.txt
test_*.md
*.test
*.tmp
